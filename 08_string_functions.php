<?php
    $string = 'Hello World';

    // Getting the length of the string.
    // echo strlen($string);

    // Find the position of the first occurence of a substring in a string.
    // echo strpos($string, 'e');

    // Find the position of the last occurence of a substring in a string.
    // echo strrpos($string, 'o');

    // Reverse the string.
    // echo strrev($string);

    // Convert all character into lowercase.
    // echo strtolower($string);

    // Convert all character into uppercase.
    // echo strtoupper($string);

    // Uppercase the first character of each words.
    // echo ucwords($string);

    // String replace.
    // echo str_replace('Hello', 'Hi', $string);

    // Return portion of a string specified by the offset and length
    // echo substr($string, 0, 5);
    // echo substr($string, 5);

    // Starts with
    // if (str_starts_with($string, 'H')) { 
    //     echo 'YES';
    // };

    // Ends with
    // if (str_ends_with($string, 'World')) {
    //     echo 'YES';
    // }

    // We can also use html entity.
    // $string2 = '<h1>Hello</h1>';
    // echo $string2;
    
    // We can also use htmlspecialchars to spellout the html entity and convert is as a text.
    // $string2 = '<h1>Hello</h1>';
    // echo htmlspecialchars($string2);

    // printf('%s likes to %s', 'Brad', 'code');

    // printf() function is use to add a text to another text.
    // printf('1 + 1 = %d', 1 + 1); // %d is use to print  a whole number.
    // printf('1 + 1 = %f', 1 + 1); // %f is use to print  a floating number.



?>